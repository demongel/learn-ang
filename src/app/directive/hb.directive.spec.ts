import { HbDirective } from './hb.directive';

describe('HbDirective', () => {
  it('should create an instance', () => {
    const directive = new HbDirective();
    expect(directive).toBeTruthy();
  });
});
